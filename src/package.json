{
  "name": "monorepo-boilerplate",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:client-dev": "yarn workspace client run dev",
    "start:server-dev": "yarn workspace server start:dev"
  },
  "devDependencies": {
    "syncpack": "^13.0.0"
  }
}
